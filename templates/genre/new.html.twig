{% extends 'base.html.twig' %}

{% block title %}New Genre{% endblock %}

{% block body %}
    <h1 class="btn btn-success">Create new Genre</h1>

    {{ include('genre/_form.html.twig') }}

    <a href="{{ path('genre_index') }}" class="btn btn-primary">back to list</a>
{% endblock %}
